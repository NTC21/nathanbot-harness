# Overview

> Bounded, always-loaded context (keep under ~2000 characters). This is the one file every
> session reads first. Edit it to describe yourself, your work, and your hard rules.
> Depth lives in the wiki and workspace MEMORY files — pull those on demand, not here.

## Who
One or two lines: who you are, what you do, what you're optimizing for.

## Active work
- **Project A** — one line on what it is and its current state.
- **Project B** — …

## Hard rules
- Never send email or create calendar events without explicit approval of the text AND the account.
- Secrets never live in this repo — they live in `~/.secrets/`.
- Ask before assuming. Name assumptions in the output.

## Pointers
- `wiki/index.md` → the durable knowledge graph. Self page: `wiki/pages/owner.md`.
- `workspace-*/MEMORY.md` → per-domain curated facts.
- `config/` → identities, projects, permissions (copy the `*.example.json` files first).
